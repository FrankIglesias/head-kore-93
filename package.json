{
  "name": "head-kore-93",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "biome check . && stylelint \"src/**/*.css\"",
    "lint:fix": "biome check --write . && stylelint \"src/**/*.css\" --fix",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "stylelint": "^17.13.0",
    "stylelint-config-standard": "^40.0.0",
    "vite": "^6.0.0"
  }
}
